# Docker_TwitterAPI_HW

Install Docker.
Clone the repository.
In the project directory, run the commands -
$ docker-compose build
$ docker-compose up
In the browser give the path : http://localhost:8000/TwitterApi/TheSeekers/
